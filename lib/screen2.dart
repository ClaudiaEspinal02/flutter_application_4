import 'package:flutter/material.dart';

class Screen2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('El cambio de monedas'),
      ),
      body: Center(
        child: Text('El cambio de monedas'),
      ),
    );
  }
}
